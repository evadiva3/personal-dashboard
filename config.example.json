{
  "_comment": "Reference only — this file is never read by the app. Canvas credentials and Google Calendar OAuth credentials are entered through the in-app onboarding/Settings screens and stored via Tauri's secure store in the OS app-data directory, never in this repo.",
  "canvas_domain": "school.instructure.com",
  "canvas_token": "REPLACE_WITH_YOUR_PERSONAL_ACCESS_TOKEN",
  "poll_interval_minutes": 30,
  "google_client_id": "REPLACE_WITH_YOUR_GOOGLE_OAUTH_CLIENT_ID.apps.googleusercontent.com",
  "google_client_secret": "REPLACE_WITH_YOUR_GOOGLE_OAUTH_CLIENT_SECRET",
  "calendar_poll_interval_minutes": 10
}
